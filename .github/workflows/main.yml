name: Build Kivy Android APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-24.04 # Upgraded to Ubuntu 24.04 which has native Node 24 support

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4 # Fetches latest node24 compatible patch cleanly

    - name: Setup System Compilers
      run: |
        sudo apt update
        sudo apt install -y git zip unzip openjdk-17-jdk python3-pip autoconf libtool pkg-config zlib1g-dev libncurses5-dev libssl-dev cmake

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        update-environment-variables: true

    - name: Install Buildozer Dependencies
      run: |
        pip install --upgrade pip
        pip install buildozer cython kivy

    - name: Build Android APK with Buildozer
      run: |
        buildozer android debug
      env:
        APP_ANDROID_ACCEPT_SDK_LICENSE: "1" 

    - name: Upload Finished APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: MofidApp-Android-Package
        path: bin/*.apk
